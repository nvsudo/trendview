<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Ignition Clean" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-surface">
    <% if authenticated? %>
      <%= render "shared/sidebar" %>

      <!-- Top Navigation Bar (Minimal - Market Data & User Controls) -->
      <nav class="bg-container shadow-sm border-b border-border fixed top-0 right-0 left-0 lg:left-64 z-40">
        <div class="px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between items-center h-16">
            <!-- Left: Burger Menu (mobile) + Market Tickers -->
            <div class="flex items-center gap-4">
              <!-- Burger Menu Button (mobile only) -->
              <button
                data-action="click->sidebar#toggle"
                class="lg:hidden text-secondary hover:text-primary p-2"
              >
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                </svg>
              </button>

              <!-- Market Tickers -->
              <div class="flex items-center gap-4 text-sm">
                <div class="flex items-center gap-2">
                  <span class="text-secondary">NIFTY 50</span>
                  <span class="text-primary font-medium">19,674.25</span>
                  <span class="text-green-600 text-xs">+0.96%</span>
                </div>
                <div class="hidden sm:flex items-center gap-2">
                  <span class="text-secondary">SENSEX</span>
                  <span class="text-primary font-medium">65,432.10</span>
                  <span class="text-green-600 text-xs">+0.38%</span>
                </div>
              </div>
            </div>

            <!-- Right: Account Selector, Notifications, Settings, User Menu -->
            <div class="flex items-center gap-3">
              <!-- Account Selector Dropdown -->
              <div class="relative">
                <button class="flex items-center gap-2 px-3 py-2 text-sm text-secondary hover:text-primary hover:bg-gray-100 rounded-lg transition-colors">
                  <span class="hidden sm:inline">All Accounts</span>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
                  </svg>
                </button>
              </div>

              <!-- Notifications -->
              <button class="relative p-2 text-secondary hover:text-primary hover:bg-gray-100 rounded-lg transition-colors">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/>
                </svg>
                <!-- Notification badge -->
                <span class="absolute top-1 right-1 w-2 h-2 bg-red-500 rounded-full"></span>
              </button>

              <!-- Settings -->
              <%= link_to "#", class: "p-2 text-secondary hover:text-primary hover:bg-gray-100 rounded-lg transition-colors" do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                </svg>
              <% end %>

              <!-- User Menu (avatar) -->
              <div class="relative">
                <button class="flex items-center gap-2 p-1 hover:bg-gray-100 rounded-lg transition-colors">
                  <div class="w-8 h-8 rounded-full bg-blue-500 flex items-center justify-center text-white text-sm font-semibold">
                    <%= current_user.initials %>
                  </div>
                </button>
              </div>
            </div>
          </div>
        </div>
      </nav>

      <!-- Main Content Area (with top padding for fixed nav and left margin for sidebar) -->
      <main class="pt-16 lg:pl-64 min-h-screen">
        <% if notice %>
          <div class="bg-success-container border border-success text-success-content px-4 py-3 rounded mb-4 mx-4 mt-4">
            <%= notice %>
          </div>
        <% end %>

        <% if alert %>
          <div class="bg-destructive-container border border-destructive text-destructive-content px-4 py-3 rounded mb-4 mx-4 mt-4">
            <%= alert %>
          </div>
        <% end %>

        <%= yield %>
      </main>
    <% else %>
      <!-- Unauthenticated Layout (Login/Register pages) -->
      <main>
        <% if notice %>
          <div class="bg-success-container border border-success text-success-content px-4 py-3 rounded mb-4 mx-4 mt-4">
            <%= notice %>
          </div>
        <% end %>

        <% if alert %>
          <div class="bg-destructive-container border border-destructive text-destructive-content px-4 py-3 rounded mb-4 mx-4 mt-4">
            <%= alert %>
          </div>
        <% end %>

        <%= yield %>
      </main>
    <% end %>
  </body>
</html>
